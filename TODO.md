# TODO: 자동 업데이트 기능

> 마지막으로 추가할 기능. 아직 구현 전 — 아래는 설계 합의 내용을 기록한 문서.

## 1. 개요

MarkDownEditor는 GitHub 릴리즈로 배포되는 포터블 단일 exe 앱이다. 새 릴리즈가 올라오면
앱이 자동으로 감지해 사용자에게 알리고, 사용자가 원하면 앱 안에서 바로 업데이트를
받아 적용할 수 있어야 한다.

## 2. UI

- **버튼 위치**: 타이틀 텍스트("MD MarkDownEditor") 바로 오른쪽. 원형 배지, 아이콘은
  느낌표(!) 컨셉.
- **업데이트 알림**: 새 버전이 감지되면 이 버튼 자체를 하이라이트(강조 색상/펄스 등)한다.
  별도 배너나 팝업을 즉시 띄우지 않고, 버튼 상태 변화로만 알린다.
- **클릭 시 팝업**: 다음 내용을 담은 팝업을 띄운다.
  - 현재 버전 / 최신 버전
  - 릴리즈 노트(GitHub 릴리즈 본문)
  - **지금 확인** 버튼 — 수동 재확인(강제 새로고침)
  - **업데이트하기** 버튼 — 새 버전이 있을 때만 활성화, 클릭 시 다운로드·적용 진행
  - 진행 중에는 다운로드 진행률 표시

## 3. 업데이트 확인 로직

- **시점**: 앱 시작 시 자동 1회(백그라운드, 실패해도 조용히 무시) + 팝업 내 "지금 확인"
  버튼으로 언제든 수동 재확인.
  - 단일 인스턴스 구조(뮤텍스+파이프)이므로 확인은 **창을 만드는 인스턴스에서만** 실행
    (파일 경로만 전달하고 즉시 종료하는 프로세스에서는 실행하지 않음).
- **API**: `GET https://api.github.com/repos/jjw1270/MarkdownEditor/releases/latest`
  (레포가 공개 상태라 인증 불필요, 비로그인 호출은 IP당 시간당 60회 제한 — 이 용도로는
  충분).
  - GitHub API는 `User-Agent` 헤더가 없으면 403을 반환함 → **요청에 User-Agent 필수**.
  - 릴리즈가 하나도 없으면 404가 옴(2026-07 현재 실제로 릴리즈 0개) → 404는 "확인 실패"가
    아니라 **"업데이트 없음"으로 처리**. "지금 확인"을 눌렀을 때도 실패 메시지가 아닌
    "최신 버전입니다"를 표시.
- **버전 비교**: 릴리즈 `tag_name`을 `vX.Y.Z` 형식으로 고정하고, 앱의 현재 버전
  (`MarkDownEditor.csproj`의 `<Version>`, 현재 `1.0.0`)과 시맨틱 버전으로 비교.
  → **릴리즈를 만들 때마다 태그를 `vX.Y.Z` 규칙으로 지켜야 함** (첫 릴리즈부터 적용).
- 네트워크 오류/레이트리밋/파싱 실패는 전부 조용히 무시(에러로 사용자를 방해하지 않음).
  단, 팝업을 열어 "지금 확인"을 눌렀는데 실패한 경우엔 팝업 안에 실패 메시지 표시.
- 테스트 편의를 위해 확인 URL을 디버그 빌드에서 오버라이드할 수 있게 해둘 것
  (E2E 검증에 실제 릴리즈 2개를 만들지 않아도 되도록).

## 4. 업데이트 적용 로직 (인앱 자동 다운로드 + 재시작 교체)

1. "업데이트하기" 클릭 → 릴리즈 자산(zip) 다운로드 (`%TEMP%` 하위 임시 폴더), 진행률 표시.
   시작 전에 `%TEMP%` 여유 공간 확인(zip + 압축 해제분, 현재 기준 약 900MB 필요).
2. 다운로드한 zip을 임시 폴더에 압축 해제.
3. 도우미 스크립트(배치/PowerShell)를 임시 폴더에 생성:
   - 메인 프로세스(PID) 종료 대기
   - 압축 해제된 `MarkDownEditor.exe`, `Runtime\`, `web\` 세 항목만 앱 폴더에 반영.
     복사-덮어쓰기가 아니라 **폴더 단위 교체**(기존 폴더를 임시 이름으로 rename 후 새
     폴더를 이동, 성공 시 구 폴더 삭제) — 덮어쓰기는 새 버전에서 삭제된 파일이 잔류하고,
     중단 시 반쯤 섞인 상태가 남기 때문.
     (**`WebView2Data\`는 절대 건드리지 않음** — 여기에 언어/테마 설정과 세션·최근 문서
     localStorage가 들어있음)
   - WebView2의 `msedgewebview2.exe` 자식 프로세스가 메인 PID 종료 후에도 잠깐 살아남아
     `Runtime\`을 잠글 수 있음 → 교체 실패 시 **짧은 간격으로 수 회 재시도**.
   - `MarkDownEditor.exe` 재실행 후 자기 자신(도우미 스크립트) 삭제
4. 앱은 **평소의 종료 절차(미저장 변경 확인 포함)를 그대로 거친 뒤**, 종료가 **확정된
   시점**(예: `Closed` 이벤트)에 도우미 스크립트를 분리 프로세스로 실행하고 종료한다.
   - **주의**: 스크립트를 먼저 띄우고 나서 종료 확인을 하면 안 됨 — 사용자가 확인 창에서
     "아니요"를 눌러 종료를 취소해도 스크립트가 백그라운드에서 PID 종료를 계속 기다리다가,
     나중에 사용자가 평범하게 앱을 닫는 순간 예고 없이 파일을 교체해 버린다.

### 릴리즈 자산 규약

- 릴리즈에 첨부하는 zip은 현재 배포 스크립트와 동일하게 `MarkDownEditor.exe`, `Runtime`,
  `web` 세 항목만 포함 (예: `MarkDownEditor-standalone.zip`).
- 자산 파일명을 고정해서 앱이 `assets[]`에서 찾을 수 있게 한다.
- **용량 문제**: 현재 zip은 약 323MB인데 대부분이 `Runtime\`(압축 전 552MB, WebView2 고정
  버전)이고 이 폴더는 거의 바뀌지 않는다. 매 업데이트마다 323MB를 받게 하는 대신,
  exe+web만 담은 별도 업데이트 자산(예: `MarkDownEditor-update.zip`)을 두고 Runtime이
  바뀐 릴리즈에서만 전체 zip을 받도록 하는 방안을 검토할 것.

### 알려진 리스크 / 열린 문제

- 앱이 쓰기 권한 없는 폴더(예: `Program Files`)에 있으면 자체 교체가 실패한다 → 이 경우
  브라우저로 릴리즈 페이지를 여는 폴백 필요.
- 도우미 스크립트(.bat/.ps1)가 백신에 오탐될 가능성 → 필요 시 안내 문구 추가 검토.
- GitHub가 다운로드 무결성(체크섬)을 기본 제공하지 않음 → 릴리즈 노트에 SHA256을 적어
  다운로드 후 검증하는 방식을 검토할 것.
- 업데이트 도중 앱이 강제 종료되면 앱 폴더가 일부만 교체된 상태로 남을 수 있음 → 4절의
  폴더 단위 교체(rename 후 삭제) 방식으로 최소화하되, 교체 순서(exe를 마지막에)와 실패 시
  구 폴더 복원까지 스크립트에 설계할 것.

## 5. 검증 범위

이 기능은 기존 기능들과 구분되므로, 구현 후에는 **이 기능만** 별도로 검증하면 된다
(다른 회귀 테스트 불필요):

- 새 버전 감지 시 버튼 하이라이트
- 팝업의 현재/최신 버전 표시, 릴리즈 노트 표시
- "지금 확인" 수동 재확인 동작
- 릴리즈가 없거나(404) 최신 버전일 때 "지금 확인" 결과가 실패가 아닌 "최신 버전"으로
  표시되는지 확인
- 최신 버전일 때 "업데이트하기" 비활성화 상태
- 다운로드 → 적용 → 재시작 후 정상 실행, 버전 갱신 확인
- 재시작 후 `WebView2Data`(언어/테마/세션/최근 문서)가 보존되는지 확인
- 미저장 변경이 있을 때 업데이트를 눌러도 평소와 동일한 종료 확인 절차를 거치는지 확인
- **종료 확인에서 "아니요"를 눌러 취소한 경우**: 도우미 스크립트가 실행되지 않고, 이후
  평범하게 앱을 닫아도 파일이 교체되지 않는지 확인
- E2E 검증은 실제 릴리즈 2개가 필요하므로, 디버그 빌드의 확인 URL 오버라이드(3절)로
  대부분의 시나리오를 먼저 검증하고, 실제 릴리즈로는 최종 1회만 확인
